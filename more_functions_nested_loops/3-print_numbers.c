#include "main.h"

/**
 * print_numbers - check the code.
 *
 * Return: Nothing
 */
void print_numbers(void)
{
	int count;

	for (count = 0; count < 10; count++)
		_putchar(count + '0');
	_putchar('\n');
}
